tasks.withType(Jar) {
	compileJava.options.encoding = 'UTF-8'
	compileApiJava.options.encoding = 'UTF-8'
}

dependencies {
	compile project(':GingaCore')
	testCompile(fileTree(dir: 'lib', include: '*.jar'))
}

println("OK");


sourceSets.main {
	java.srcDirs project.projectDir.name
	resources.srcDirs project.projectDir.name
	resources.exclude 'dats'
}

version = "2.0-beta1"

jar {
	manifest {
		attributes 'Main-Class': 'jp.gingarenpo.gtc.directly.Main'
	}
}